name: Test wf_test

on:
  workflow_dispatch:

jobs:
  job1:
    name: run it
    runs-on: ubuntu-latest
    steps:
      - id: s1
        uses: hsolbrig/actions/wf_test@main
      - run: echo ${{ s1.outputs.reply }}
